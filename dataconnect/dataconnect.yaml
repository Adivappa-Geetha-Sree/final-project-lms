specVersion: "v1beta"
serviceId: "lms-admin-pannel"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "lms-admin-pannel-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
